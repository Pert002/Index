import React from 'react';
import Carousel from "../Carousel";
const Info = () => {
    return (
        <div className="info__carousel">
            <Carousel />
        </div>
    );
};

export default Info;